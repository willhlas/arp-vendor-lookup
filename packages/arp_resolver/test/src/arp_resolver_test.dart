// ignore_for_file: prefer_const_constructors
import 'package:arp_resolver/arp_resolver.dart';
import 'package:test/test.dart';

void main() {
  group('ArpResolver', () {
    test('can be instantiated', () {
      expect(ArpResolver(), isNotNull);
    });
  });
}
